# Expensive-Tracker
 A simple console-based Expense Tracker application built using Java, JDBC, and MySQL.   This project allows users to manage daily expenses with features like adding, viewing, and deleting expense records.   The data is stored in a MySQL database using secure CRUD operations.
